#include "Particle.hpp"

// Definitions of constructors
Particle::Particle(double q_in, double m_in, arma::vec v_in, arma::vec r_in)
: q(q_in), m(m_in), v(v_in), r(r_in)
{
  //double q_ = q;      // charge (e)
  //double m_ = m;      // mass (u)
  //arma::vec r_ = r;   // position (x,y,z)
  //arma::vec v_ = v;   // velocity (vx,vy,vz)
}



